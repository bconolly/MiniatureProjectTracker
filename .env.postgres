# PostgreSQL environment configuration
# For testing with PostgreSQL locally (similar to AWS setup)

# Database configuration - PostgreSQL
DATABASE_URL=postgresql://tracker_user:tracker_pass@localhost:5432/miniature_tracker

# Server configuration
PORT=3000

# Storage configuration - Local filesystem (S3 simulation)
STORAGE_TYPE=local
LOCAL_STORAGE_PATH=./uploads

# Logging level
RUST_LOG=info

# CORS origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173