# Docker environment configuration
# For running the application in Docker containers

# Database configuration - SQLite in container
DATABASE_URL=sqlite:./miniature_tracker.db

# Server configuration
PORT=3000

# Storage configuration - Local filesystem in container
STORAGE_TYPE=local
LOCAL_STORAGE_PATH=./uploads

# Logging level
RUST_LOG=info

# CORS origins (for Docker setup)
CORS_ORIGINS=http://localhost:3000